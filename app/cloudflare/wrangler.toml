name = "pcg-mas-demo"
compatibility_date = "2026-01-01"
pages_build_output_dir = "../frontend"

# No secrets here. The frontend is a static bundle and must never embed a provider key.
[vars]
PCG_MAS_RELEASE = "v3.0"
