# Experiment specification

The v3.7 numerical authority contains 7 models, 8 datasets, 56 model–dataset cells, 40 candidate generations per cell, and 392 method–cell aggregate rows. Preserved v3.6 generation and execution evidence is admitted only through identity and hash reconciliation. Current per-observation verifier values, decisions, and outcome labels were not retained and are never reconstructed from aggregates. The embedded manuscript stack is the sole source for v3.7 reported numbers and artifacts.
